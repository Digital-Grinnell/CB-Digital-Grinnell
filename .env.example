# Azure Storage CLI defaults for this project.
# Authenticate interactively with `az login`; do not add keys or SAS tokens here.
export AZURE_STORAGE_ACCOUNT="digitalgrinnell"
export AZURE_STORAGE_AUTH_MODE="login"
